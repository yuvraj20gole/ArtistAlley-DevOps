pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Checkout stage completed'
            }
        }

        stage('Build') {
            steps {
                echo 'Build stage completed'
            }
        }

        stage('Test') {
            steps {
                echo 'Test stage completed'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy stage completed (simulated)'
            }
        }
    }
}
